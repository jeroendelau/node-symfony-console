
  const mocha = require('mocha');
  const chai = require('chai');
  const {assert} = chai;
  
  

  // const StreamableInputInterface = require('../../src/input/StreamableInputInterface');
  
  describe('#AbstractQuestionHelper',() => {
    
    /**
        function createStreamableInputInterfaceMock() {
              let mock = this.getMockBuilder(StreamableInputInterface.class).getMock();
        mock.expects(this.any())
            .method('isInteractive')
            .willReturn(interactive);

        if (stream) {
            mock.expects(this.any())
                .method('getStream')
                .willReturn(stream);
        }

        return mock;
    
    }
     **/
  });
  